<%= form_with(model: board_user, local: true) do |form| %>
  <% if board_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(board_user.errors.count, "error") %> prohibited this board_user from being saved:</h2>

      <ul>
      <% board_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table>
  <tr class="field">
    <th>ニックネーム</th>
    <td><%= form.text_field :nickname, id: :board_user_nickname %></td>
  </tr>

  <%= form.hidden_field :account_id %>

  <tr class="field">
    <th>自己紹介</th>
    <td><%= form.text_area :memo, id: :board_user_memo %></td>
  </tr>

  <tr class="actions">
    <th></th><td><%= form.submit value: "変更" %></td>
  </tr>
  </table>
<% end %>
